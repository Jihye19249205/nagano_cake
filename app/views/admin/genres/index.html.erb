

<h3>ジャンル一覧・追加</h3>
<%= form_with url: admin_genres_path do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <!--bootstrap button succes↓-->
  <%= f.submit '新規登録' %>
<% end %>
<% @genres.each do |genre| %>
<table>
  <thead>
    <tr>
      <th>ジャンル</th>
      <th colspan = "1"></th>
    </tr>
  </thead>
</table>
<% end %>

